name: CI
'on':
  push: null
  pull_request: null
jobs:
  artifact-check:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Run artifact check
        run: scripts\check_artifact.cmd